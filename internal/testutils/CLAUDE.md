# internal/testutils

## Purpose

Deterministic key generation, transaction builders, and test fixture helpers
for unit and integration tests.

## Relationships

- **Depends on**: nothing (leaf test utility).
- **Depended on by**: test files across `rounds`, `oor`, `batch`, `db`, etc.
