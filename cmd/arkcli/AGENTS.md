# cmd/arkcli

## Purpose

Admin CLI for the Ark operator server. Provides commands to trigger batches,
list rounds, query state, and manage the running daemon.

## Relationships

- **Depends on**: `adminrpc` (gRPC client stubs).
- **Depended on by**: nothing (top-level binary).
