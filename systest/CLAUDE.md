# systest

## Purpose

System-level end-to-end tests running the full operator daemon with real chain
backends and client connections.

## Relationships

- **Depends on**: `harness` (test environment), most server packages.
- **Depended on by**: nothing (test-only).
