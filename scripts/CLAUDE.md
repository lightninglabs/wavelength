# scripts

## Purpose

Build, code generation, and verification scripts (protobuf generation, Go
version checks, submodule management, doc validation).

## Relationships

- **Depends on**: nothing (standalone scripts).
- **Depended on by**: Makefile targets (`make rpc`, `make doc-check`, etc.).
