# rules

## Purpose

ast-grep YAML rule definitions for code style enforcement. Used by `make
ast-lint` and `make ast-grep-fix`.

## Relationships

- **Depends on**: nothing (YAML configuration files).
- **Depended on by**: `make ast-lint`, `make ast-grep-fix`.

## Deep Docs

- [docs/ast-grep-guide.md](../docs/ast-grep-guide.md) — Usage guide and rule descriptions.
